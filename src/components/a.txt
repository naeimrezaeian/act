/*
      const responseFile = await httpClient.get('/api/files/DownloadFile/' + this.fileId+"/"+this.accessCode)
      //audio/mp3
      // application/octet-stream
      var mp3 = new Blob([responseFile.data], { type: 'audio/mp3' })
      var reader = new FileReader();
      reader.readAsDataURL( mp3 );
     
      reader.onloadend = function(evt) 
            {
              console.log("get file")


              this.context.decodeAudioData(evt.target.result, (buffer) => {
                this.source = this.context.createBufferSource();
                this.source.buffer = buffer;
                this.source.connect(this.context.destination);
                this.source.start(0);


              })
              */
             /*
              let request = new XMLHttpRequest();
             request.open("GET","http://jplayer.org/audio/mp3/RioMez-01-Sleep_together.mp3",true);
             request.responseType = "arraybuffer";
             request.onload = () => {
              this.context.decodeAudioData(request.response, (buffer) => {
                this.source = this.context.createBufferSource();
                this.source.buffer = buffer;
                this.source.connect(this.context.destination);
                this.source.start(0);
              })

             }
*/
              //const audio = document.getElementById('my-audio')
              //audio.src=evt.target.result
             
              //this.audioContent="uploads/m1.mp3"
              //this.audio.src="uploads/m1.mp3"
              //this.audio.src=evt.target.result
             // this.audio.data=evt.target.result
              //this.audioContent=evt.target.result
              //this.audio.play()

             //data:audio/mp3;base64,SUQzAwAAAAxSSlRJVDIAAAAaAAAAWW
             //data:audio/mp3;base64,SUQzAwAAAAxSSlRJVDIAAAAaAAAAWW
            // var audio = new Audio(evt.target.result)
              //audio.data=evt.target.result
              //audio.src="uploads/m1.mp3"
              //audio.play()              
              //audio.data=evt.target.result                 
              //console.log(audio.data)
              //let bytes_mp3 = new Uint8Array( evt.target.result ); 
              
             
           // }.bind(this)

      //var audio = new Audio("uploads/m1.mp3")
       // audio.play()
    //  const responseFile = await httpClient.get('/api/files/DownloadFile/' + this.fileId+"/"+this.accessCode)
      //var blob = new Blob(responseFile.data)
      //var mp3 = new Blob([responseFile.data], { type: 'application/octet-stream' })
      //var url = (window.URL || window.webkitURL).createObjectURL( mp3 );
     
   //   var reader = new FileReader();
    //  reader.readAsDataURL(mp3);

      //reader.onload = function() {
       

//};
            